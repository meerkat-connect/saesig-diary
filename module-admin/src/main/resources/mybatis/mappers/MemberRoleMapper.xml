<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.saesig.role.MemberRoleMapper">

    <insert id="insertMemberRoles">
        INSERT INTO member_role(
            member_id
            , role_id
            , modified_at
            , modified_by
            , created_at
            , created_by
        ) VALUES
        <foreach collection="memberIds" item="memberId" separator=",">
            (#{memberId}, #{roleId}, now(), #{sessionMemberId}, now(), #{sessionMemberId})
        </foreach>
    </insert>
</mapper>