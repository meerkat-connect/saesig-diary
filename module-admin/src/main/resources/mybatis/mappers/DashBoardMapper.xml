<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.saesig.index.DashBoardMapper">
    <sql id="whereCondition">
        <if test="periodType != null and periodType =='monthly'">
            YEAR(${created_at}) = YEAR(NOW()) AND MONTH(${created_at}) = MONTH(now())
        </if>
        <if test="periodType != null and periodType =='daily'">
            DATE(${created_at}) = DATE(NOW())
        </if>
        <if test="periodType != null and periodType =='weekly'">
            YEARWEEK(${created_at}, 1) = YEARWEEK(NOW(), 1)
        </if>
    </sql>

    <sql id="wherePrevCondition">
        <if test="periodType != null and periodType =='monthly'">
            YEAR(${created_at}) = YEAR(NOW() - INTERVAL 1 MONTH) AND MONTH(${created_at}) = MONTH(NOW()- INTERVAL 1 MONTH)
        </if>
        <if test="periodType != null and periodType =='daily'">
            DATE(${created_at}) = DATE(NOW() - INTERVAL 1 DAY)
        </if>
        <if test="periodType != null and periodType =='weekly'">
            YEARWEEK(${created_at}, 1) = YEARWEEK(NOW() - INTERVAL 1 WEEK, 1)
        </if>
    </sql>

    <!-- 새로운식구 조회-->
    <select id="selectAdoptions" resultType="map">
        SELECT
            adopt.id
            , adopt.title
            , adopt.image_file_group_id
            , adopt.sido
            , adopt.sigungu
            , adopt.gender
            , adopt.animal_division1_id
            , adopt.animal_division2_id
            , animal_division1.category
            , animal_division2.category
        FROM adopt
        LEFT OUTER JOIN animal_division1
            ON adopt.animal_division1_id = animal_division1.id
        LEFT OUTER JOIN animal_division2
            ON adopt.animal_division2_id = animal_division2.id
        ORDER BY created_by DESC
        LIMIT 3
    </select>

    <!-- 일상기록 조회 -->
    <select id="selectDiarys" resultType="map">
        SELECT
            image_file_group_id
        FROM diary
        ORDER BY created_by DESC
        LIMIT 6
    </select>

    <!-- 관리자 게시판 조회 -->
    <select id="selectAdminPosts" resultType="com.saesig.managerBoard.ManagerBoardDto">
        SELECT
            post.id
            , post.category
            , post.title
            , post.created_at
        FROM
            post
        INNER JOIN board
            ON post.board_id = board.id
        WHERE board.category = 'ADMIN'
        ORDER BY created_at DESC
        LIMIT 4
    </select>

    <!--회원 가입 -->
    <select id="countRegisteredMembers" resultType="map">
        <if test="periodType == 'daily'">
            SELECT
                IFNULL(SUM(CASE WHEN DATE(created_at) <![CDATA[<]]>  CURDATE() THEN 1 ELSE 0 END), 0) yesterday
                , IFNULL(SUM(CASE WHEN DATE(created_at) >= CURDATE() THEN 1 ELSE 0 END), 0) today
            FROM member
            WHERE
                DATE(created_at) = CURDATE()
                AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)
        </if>
        <if test="periodType == 'weekly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE(), 3) THEN 1 ELSE 0 END), 0) this_week
                , IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3) THEN 1 ELSE 0 END), 0) last_week
            FROM member
            WHERE
                YEARWEEK(created_at, 3) >= YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3)
        </if>
        <if test="periodType == 'monthly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()) THEN 1 ELSE 0 END), 0) this_month
                , IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH) THEN 1 ELSE 0 END), 0) last_month
            FROM member
            WHERE
                (YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()))
                OR (YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH))
        </if>

        <if test="periodType == 'totally'">
            SELECT
                IFNULL(COUNT(1), 0) AS total_count
            FROM member
        </if>
    </select>

    <!-- 발송현황 -->
    <select id="countMessageDeliverys" resultType="map">
        SELECT
            send_template.method
            , COUNT(1) as send_count
        FROM send_history
        INNER JOIN send_template
            ON send_history.send_template_id = send_template.id
        WHERE
        <include refid="whereCondition">
            <property name="created_at" value="send_history.created_at"/>
        </include>
        GROUP BY send_template.method

    </select>

    <!-- 신고현황 -->
    <select id="countReports" resultType="integer">
        <if test="periodType == 'daily'">
            SELECT
                IFNULL(SUM(yesterday), 0) as yesterday
                , IFNULL(SUM(today), 0) as today
            FROM (
                SELECT
                    SUM(CASE WHEN DATE(created_at) <![CDATA[<]]>  CURDATE() THEN 1 ELSE 0 END) yesterday
                    , SUM(CASE WHEN DATE(created_at) >= CURDATE() THEN 1 ELSE 0 END) today
                FROM adopt_report
                WHERE
                    DATE(created_at) = CURDATE()
                    AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)

                UNION ALL

                SELECT
                    SUM(CASE WHEN DATE(created_at) <![CDATA[<]]>  CURDATE() THEN 1 ELSE 0 END) yesterday
                    , SUM(CASE WHEN DATE(created_at) >= CURDATE() THEN 1 ELSE 0 END) today
                FROM diary_report
                WHERE
                    DATE(created_at) = CURDATE()
                    AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)
            ) AS subquery
        </if>

        <if test="periodType == 'weekly'">
            SELECT
                IFNULL(SUM(this_week), 0) AS this_week
                , IFNULL(SUM(last_week), 0) AS last_week
            FROM (
                SELECT
                    SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE(), 3) THEN 1 ELSE 0 END) this_week,
                    SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3) THEN 1 ELSE 0 END) last_week
                FROM adopt_report
                WHERE
                    YEARWEEK(created_at, 3) >= YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3)

                UNION ALL

                SELECT
                    SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE(), 3) THEN 1 ELSE 0 END) this_week,
                    SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3) THEN 1 ELSE 0 END) last_week
                FROM diary_report
                WHERE
                    YEARWEEK(created_at, 3) >= YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3)
            ) AS subquery
        </if>
        <if test="periodType == 'monthly'">
            SELECT
                IFNULL(SUM(this_month), 0) AS this_month
                , IFNULL(SUM(last_month), 0) AS last_month
            FROM (
                SELECT
                    SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()) THEN 1 ELSE 0 END) this_month,
                    SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH) THEN 1 ELSE 0 END) last_month
                FROM adopt_report
                WHERE
                    (YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()))
                    OR (YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH))

                UNION ALL

                SELECT
                    SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()) THEN 1 ELSE 0 END) this_month,
                    SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH) THEN 1 ELSE 0 END) last_month
                FROM diary_report
                WHERE
                    (YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()))
                    OR (YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH))
            ) AS subquery
        </if>

        <if test="periodType == 'totally'">
            SELECT
                IFNULL(COUNT(total), 0) AS total
            FROM (
                SELECT
                    COUNT(1) total
                FROM adopt_report

                UNION ALL

                SELECT
                    COUNT(1) total
                FROM diary_report
            ) AS subquery
        </if>
    </select>

    <!-- 문의 현황 -->
    <select id="countInquiries" resultType="integer">
        <if test="periodType == 'daily'">
            SELECT
                IFNULL(SUM(CASE WHEN DATE(created_at) <![CDATA[<]]>  CURDATE() THEN 1 ELSE 0 END), 0) AS yesterday
                , IFNULL(SUM(CASE WHEN DATE(created_at) >= CURDATE() THEN 1 ELSE 0 END), 0) AS today
            FROM inquiry
            WHERE
                DATE(created_at) = CURDATE()
                AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)
        </if>
        <if test="periodType == 'weekly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE(), 3) THEN 1 ELSE 0 END), 0) AS this_week
                , IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3) THEN 1 ELSE 0 END), 0) AS last_week
            FROM inquiry
            WHERE
                YEARWEEK(created_at, 3) >= YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3)
        </if>
        <if test="periodType == 'monthly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()) THEN 1 ELSE 0 END), 0) AS this_month
                , IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH) THEN 1 ELSE 0 END), 0) AS last_month
            FROM inquiry
            WHERE
                (YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()))
                OR (YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH))
        </if>

        <if test="periodType == 'totally'">
            SELECT
                IFNULL(COUNT(1), 0)
            FROM inquiry
        </if>
    </select>

    <!-- 분양완료 현황 -->
    <select id="countAdoptions" resultType="integer">
        <if test="periodType == 'daily'">
            SELECT
                IFNULL(SUM(CASE WHEN DATE(created_at) <![CDATA[<]]>  CURDATE() THEN 1 ELSE 0 END), 0) AS yesterday
                , IFNULL(SUM(CASE WHEN DATE(created_at) >= CURDATE() THEN 1 ELSE 0 END), 0) AS today
            FROM adopt
            WHERE
                status = 'COMPLETE'
                AND DATE(created_at) = CURDATE()
                AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)
        </if>
        <if test="periodType == 'weekly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE(), 3) THEN 1 ELSE 0 END), 0) AS this_week
                , IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3) THEN 1 ELSE 0 END), 0) AS last_week
            FROM adopt
            WHERE
                status = 'COMPLETE'
                AND YEARWEEK(created_at, 3) >= YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3)
        </if>
        <if test="periodType == 'monthly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()) THEN 1 ELSE 0 END), 0) AS this_month
                , IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH) THEN 1 ELSE 0 END), 0) AS last_month
            FROM adopt
            WHERE
                status = 'COMPLETE'
                AND (
                    (YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()))
                    OR (YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH))
                )
        </if>

        <if test="periodType == 'totally'">
            SELECT
                IFNULL(COUNT(1), 0) AS total_count
            FROM adopt
            WHERE
                status = 'COMPLETE'
        </if>
    </select>

    <!-- 일기 현황 -->
    <select id="countDiarys" resultType="integer">
        <if test="periodType == 'daily'">
            SELECT
                IFNULL(SUM(CASE WHEN DATE(created_at) <![CDATA[<]]>  CURDATE() THEN 1 ELSE 0 END), 0) yesterday
                , IFNULL(SUM(CASE WHEN DATE(created_at) >= CURDATE() THEN 1 ELSE 0 END), 0) today
            FROM diary
            WHERE
                DATE(created_at) = CURDATE()
                AND DATE(created_at) = DATE_SUB(CURDATE(), INTERVAL 1 DAY)
        </if>
        <if test="periodType == 'weekly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE(), 3) THEN 1 ELSE 0 END), 0) this_week
                , IFNULL(SUM(CASE WHEN YEARWEEK(created_at, 3) = YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3) THEN 1 ELSE 0 END), 0) last_week
            FROM diary
            WHERE
                YEARWEEK(created_at, 3) >= YEARWEEK(CURDATE() - INTERVAL 7 DAY, 3)
        </if>
        <if test="periodType == 'monthly'">
            SELECT
                IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()) THEN 1 ELSE 0 END), 0) this_month
                , IFNULL(SUM(CASE WHEN YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH) THEN 1 ELSE 0 END), 0) last_month
            FROM diary
            WHERE
                (YEAR(created_at) = YEAR(CURDATE()) AND MONTH(created_at) = MONTH(CURDATE()))
                OR (YEAR(created_at) = YEAR(CURDATE() - INTERVAL 1 MONTH) AND MONTH(created_at) = MONTH(CURDATE() - INTERVAL 1 MONTH))
        </if>

        <if test="periodType == 'totally'">
            SELECT
                COUNT(1)
            FROM diary
        </if>
    </select>

    <select id="countAdoptionStatus" resultType="integer">
        SELECT
            months.month
            , COALESCE(COUNT(adopt.id), 0) AS count
        FROM (
            SELECT 1 AS month UNION ALL SELECT 2 UNION ALL SELECT 3 UNION ALL
            SELECT 4 UNION ALL SELECT 5 UNION ALL SELECT 6 UNION ALL
            SELECT 7 UNION ALL SELECT 8 UNION ALL SELECT 9 UNION ALL
            SELECT 10 UNION ALL SELECT 11 UNION ALL SELECT 12
        ) months
        LEFT OUTER JOIN adopt
            ON MONTH(adopt.created_at) = months.month
                AND YEAR(adopt.created_at) = #{searchAdoptionYear}
                AND adopt.status = #{searchAdoptionStatus}
        GROUP BY months.month
    </select>

    <select id="selectAdoption" resultType="map">

    </select>
</mapper>